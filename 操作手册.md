# 知谱智航 — 操作手册

## 一、启动 Neo4j

> 以下 `<你的Neo4j目录>` 替换为实际安装路径，例如 `D:\neo4j\neo4j-community-2026.02.2-windows\...`

```powershell
$NEO4J_HOME = "<你的Neo4j目录>"
$env:JAVA_HOME = "$NEO4J_HOME\jdk"
cd $NEO4J_HOME
bin\neo4j start
```

## 二、启动后端

```powershell
cd backend
python -m uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

> Swagger 文档：http://localhost:8000/docs

## 三、启动前端

```powershell
cd frontend
npm run dev
```

> 前端页面：http://localhost:5173

## 四、使用流程

### 教师端

1. 打开 http://localhost:5173 → 注册教师账号 → 登录
2. 首页点 **创建课程** → 填写课程名和简介
3. 进入课程 → 点 **上传课件文档**（支持 PDF / DOCX / MD / TXT）
4. 文档上传后点 **AI 知识提取**（约 20 秒，自动生成知识图谱）
5. 右侧查看知识点预览，点 **查看全部** 浏览所有知识点
6. 侧边栏 → **智能问答** 可向 AI 提问课程相关问题

### 学生端

1. 注册学生账号 → 登录
2. 浏览课程列表 → 进入课程查看知识图谱
3. 点 **查看学习进度** 追踪掌握状态
4. 点 **获取学习推荐** 获取 AI 推荐的学习路径
5. 侧边栏 → **智能问答** 向 AI 助教提问

## 五、关闭

```powershell
# 前端 / 后端 各自按 Ctrl+C 停止

# 停止 Neo4j
$NEO4J_HOME = "<你的Neo4j目录>"
$env:JAVA_HOME = "$NEO4J_HOME\jdk"
cd $NEO4J_HOME
bin\neo4j stop
```